"""Opt-in skill-behavior test suite for atelier.

NOT discovered by `tests/run.py` (which only lists top-level `tests/test_*.py`).
The offline assertion-engine unit tests live in `tests/test_skill_behavior_assertions.py`;
the live runner is `run_skill_behavior.py` (invoked manually).
"""
