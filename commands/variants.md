---
description: Produce 2-3 distinct, on-contract design directions with content parity
argument-hint: [what to vary]
allowed-tools: Read, Write, Edit, Bash(python3:*), Bash(node:*)
---

Produce 2-3 genuinely distinct design directions for `$ARGUMENTS` to choose between. Follow `references/capabilities/variants.md`: every variant carries identical content (parity is what makes the comparison fair) and every variant stays bound to the same DESIGN.md contract — they differ in composition, emphasis, and treatment, never in invented tokens. Run `${CLAUDE_PLUGIN_ROOT}/scripts/qa.py` on each before presenting, and show them side by side.
